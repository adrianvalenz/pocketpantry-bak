<div class="flashes" id="flash">
  <% flash.each do |type, message| %>
    <%= content_tag :div, message, class: "flash #{type} fixed top-0 left-0 right-0 text-center px-4 py-2 rounded-md bg-rose-100 text-rose-800", data: { controller: "flash" } %>
  <% end %>
</div>
