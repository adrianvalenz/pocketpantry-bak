<% content_for :content do %>
    <div class="max-w-5xl mx-auto px-4 py-8 grid grid-cols-5 gap-8">
      <nav class="col-span-1 sidebar">
        <div class="border border-rose-300 rounded-md bg-stone-50">
          <%= render 'dashboard/sidebar' %>
        </div>
      </nav>
      <div class="col-span-4">
        <%= yield %>
      </div>
    </div>
<% end %>

<%= render template: "layouts/application" %>

