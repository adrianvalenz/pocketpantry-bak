<%= turbo_frame_tag dom_id(@recipe) do %>
  <div class="max-w-full bg-stone-50 rounded-md px-4 py-2">
    <div class="bg-stone-50">
      <h2><%= t("dashboard.recipes.update_recipe") %> <%= @recipe.name %></h2>
      <%= render partial: 'form', locals: { model: @recipe, submit: "Update" } %>
    </div>
  </div>
<% end %>
